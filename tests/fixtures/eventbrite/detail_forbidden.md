Captured on 2026-09-16 from `GET /v3/events/1251397667109/` with
`expand=venue,category,organizer`. HTTP 403 followed 60 successfully staged
events in the same crawl. Only the error response fields are retained;
credentials and request headers are not included.
